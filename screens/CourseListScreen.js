import { View, Text } from 'react-native';

const CourseListScreen = () => { 
      return ( 
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}> 
            <Text>CourseList Screen</Text> 
      </View> 
      );
};

export default CourseListScreen;